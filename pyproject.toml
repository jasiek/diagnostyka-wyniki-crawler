[project]
name = "diagnostyka-wyniki-crawler"
version = "0.1.0"
description = "Crawler for downloading Diagnostyka lab results."
readme = "README.md"
requires-python = "==3.12.11"
license = { text = "MIT" }
authors = [
    { name = "diagnostyka-wyniki team", email = "info@example.com" },
]
dependencies = [
    "playwright>=1.55.0,<2",
    "python-dotenv>=1.0.0,<2",
]

[dependency-groups]
dev = [
    "pytest>=7.4,<8",
]

[tool.uv]
package = false
